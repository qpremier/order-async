name = "React Router"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"
[commands]
predev = "npm run prisma:generate"
dev = "npm run migrate:deploy && npm exec react-router dev"
